# metopt-lab1
Лабораторная работа по конструктивным действительным числам и black-box оптимизации.

Выполнили:
- Марковский Артём Владиславович, 466636, J3210
- Щербаков Артём Олегович, 468145, J3210
- Павлов Арсений Евгеньевич, 466986, J3210

Цель проекта — реализовать:

- модель конструктивных действительных чисел;
- два алгоритма оптимизации:
  - имитацию отжига;
  - генетический алгоритм;
- тестирование алгоритмов на функции Розенброка.

---

## Структура проекта

```
metopt-lab1/
├── cmd/
│   └── metopt-lab1/
│       └── main.go
├── internal/
│   ├── constructive/
│   ├── annealing/
│   ├── genetic/
│   ├── objective/
│   └── experiment/
│       ├── annealing/
│       └── genetic/
├── testdata/
├── README.md
├── .gitignore
└── go.mod
```

## Назначение директорий

`cmd/metopt-lab1` — точка входа в приложение. Здесь находится main.go, который нужен для запуска программы и объединения всех частей проекта.

`internal/constructive` — пакет для реализации конструктивных действительных чисел. В этой директории находится реализованная мной часть лабораторной работы.

`internal/annealing` — пакет для реализации метода имитации отжига.

`internal/genetic` — пакет для реализации генетического алгоритма.

`internal/objective` — пакет для целевых функций оптимизации, включая функцию Розенброка.

`internal/experiment` — пакет для сценариев запуска экспериментов, сравнения алгоритмов и вывода результатов.

`testdata` — директория для возможных данных, используемых в тестах.

`README.md` — описание проекта и текущее состояние реализации.

`.gitignore` — список файлов и директорий, которые не добавляются в Git.

`go.mod` — файл Go-модуля проекта.

---

## Реализация модели конструктивных действительных чисел

Конструктивное действительное число представляется не как просто сохранённое значение, а как объект, который умеет выдавать приближение с заданной точностью epsilon.

Это выражено через интерфейс:

```
type Real interface {
    Approx(eps float64) (float64, error)
}
```

Таким образом, любое число в данной модели должно поддерживать получение приближённого значения по запросу.

### Что реализовано

- интерфейс Real;
- создание константного конструктивного числа через NewConstant;
- создание конструктивного числа из функции приближения через NewFromApprox;
- арифметические операции:
  - Add;
  - Sub;
  - Mul;
- сравнение чисел через Compare;
- обработка некорректного значения epsilon;
- тесты для базовой функциональности, арифметических операций и сравнения.

---

## Реализация метода имитации отжига

Метод имитации отжига представляет собой цикл:
- Инициализация текущей точки и её значения
- Генерация соседней точки
- Оценка нового состояния
- Принятие или отклонение перехода
- Понижение температуры

Имитация отжига реализована как минимизатор black-box функции с поддержкой произвольной размерности и ограничений по координатам.

### Основные компоненты

**Config:**\
Задаёт параметры запуска: число итераций, температуры, скорость охлаждения, размер шага, epsilon, границы поиска и `Seed`.

**Optimize:**\
Основной алгоритм, который:
- валидирует конфигурацию и стартовую точку;
- итерируется до достижения `MaxIterations` или `MinTemperature`;
- отслеживает лучшее найденное решение.

**generateNeighbor:**\
Строит соседа случайным сдвигом каждой координаты на отрезке `[-StepSize, StepSize]` с обрезкой в границах через `clamp`.

**shouldAccept:**\
Реализует критерий Метрополиса:
- улучшение (`delta <= 0`) принимается всегда;
- ухудшение принимается с вероятностью `exp(-delta / temperature)`.

**Result:**\
Возвращает лучшую точку, лучшее значение, число итераций, число принятых переходов и финальную температуру.

### Особенности реализации

- строгая валидация входных параметров и границ области поиска;
- проверка соответствия размерности функции, границ и стартовой точки;
- воспроизводимость экспериментов через фиксированный `Seed`;
- явная остановка по одному из двух критериев: лимит итераций или охлаждение до `MinTemperature`.

### Performance метода имитации отжига

Запуск алгоритма представлен на `2D Rosenbrock` и `5D Rosenbrock` в `internal/experiment/annealing`, а сводный запуск с выводом результатов находится в `internal/experiment/performance.go`.

Параметры экспериментов:
- `SA 2D`: `MaxIterations=30000`, `InitialTemperature=8`, `CoolingRate=0.999`, `StepSize=0.2`, `Seed=7`.
- `SA 5D`: `MaxIterations=60000`, `InitialTemperature=10`, `CoolingRate=0.9995`, `StepSize=0.25`, `Seed=11`.

Пример формата вывода (как печатается в `cmd/metopt-lab1/main.go`):

```text
--- SA 2D ---
Стартовая точка:       [-1.2 1]
Стартовое значение:    24.20000000
Лучшая точка:          <best-point>
Лучшее значение:       <best-value>
Итераций:              <iterations>
Время выполнения:      <duration>

--- SA 5D ---
Стартовая точка:       [-1.2 1 1 1 1]
Стартовое значение:    24.20000000
Лучшая точка:          <best-point>
Лучшее значение:       <best-value>
Итераций:              <iterations>
Время выполнения:      <duration>
```

---

## Реализация генетического алгоритма

Генетический алгоритм представляет собой цикл:
- Начальная инициализация популяции (выполняется только один раз)
- Вычисление приспособленности
- Отбор 
- Скрещивание 
- Мутации

Генетический алгоритм реализован как гибкий и расширяемый движок, работающий с любыми числовыми типами

### Основные компоненты

**Engine:**\
Управляет pipeline'ом, выполняет сам алгоритм

**Selection:** 
- Tournament: турнирный отбор, выбирающий лучшую особь из случайной группы.
- Ranking: ранговый отбор, который позволяет самым приспособленным особям не так сильно доминировать над остальными.

**Crossover:**
- SBX (Simulated Binary Crossover): имитирует поведение одноточечного кроссовера для вещественных чисел, позволяя контролировать среднее и расстояние между потомками и родителями.
- BLX (Blend Crossover): создает потомков в расширенном диапазоне вокруг родителей.

**Mutation:**
- GaussianMutator: Добавляет к гену случайную величину, распределенную по нормальному закону, с дисперсией зависимой от границ.

**Replacement (Замена популяции):**
- Elitism: Гарантирует выживание N лучших особей из предыдущего поколения.
- Absent: Полная замена старой популяции новыми потомками.


### Особенности реализации 

- возможность находить как max, так и min функции (через параметр Weight)
- возможность расширить логику через реализации новых поведений в пакетах selection, mutation, replacement, crossover

### Performance генетического алгоритма 

Запуск алгоритма предсталвен на 2D Rosenbrock и 5D Rosenbrcok в `internal/experiment/genetic`

Также вот результаты запуска в `cmd/metopt-lab1/main.go`

```text
--- GA 2D ---
Размер популяции:      200
Поколений:             150
Лучшая точка:          [1.033973606903362 1.069343975903661]
Лучшее значение:       0.00116009
Расстояние до [1..1]:  0.07721912
Время выполнения:      8.53075ms

--- GA 5D ---
Размер популяции:      500
Поколений:             400
Лучшая точка:          [0.9050013392612686 0.8215294221599058 0.6749161927493524 0.45474126067925275 0.20540025579875282]
Лучшее значение:       0.44474148
Расстояние до [1..1]:  1.03694350
Время выполнения:      89.565417ms
```

---
